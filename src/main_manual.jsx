import React,{useEffect,useState}from'react';import{createRoot}from'react-dom/client';import{supabase}from'./supabase';import'./styles.css';
const nav=[['dashboard','⌂','Dashboard'],['materias','▣','Materias Primas'],['produccion','⚙','Órdenes de Producción'],['formulas','≡','Fórmulas'],['productos','◆','Productos Terminados'],['despachos','⇧','Despachos'],['movimientos','↻','Movimientos']];const peso={'Big Bag 1000 kg':1000,'Bolsa 50 kg':50,'Bolsa 25 kg':25};const fmt=n=>Number(n||0).toLocaleString('es-PY',{maximumFractionDigits:3});const estado=(s,m)=>Number(s||0)<=Number(m||0)?'Crítico':Number(s||0)<=Number(m||0)*1.5?'Precaución':'Óptimo';
function App(){const[tab,setTab]=useState('dashboard'),[d,setD]=useState({materias:[],products:[],formulas:[],orders:[],dispatches:[],movimientos:[]}),[loading,setLoading]=useState(true),[err,setErr]=useState(''),[modal,setModal]=useState(null),[toast,setToast]=useState('');const load=async()=>{setLoading(true);const q=await Promise.all([supabase.from('materias_primas').select('*').eq('activo',true).order('nombre'),supabase.from('productos_terminados').select('*').eq('activo',true).order('nombre'),supabase.from('formulas').select('*').eq('activo',true).order('nombre'),supabase.from('ordenes_produccion').select('*,formulas(nombre,codigo)').order('id',{ascending:false}),supabase.from('despachos').select('*,productos_terminados(nombre,codigo)').order('id',{ascending:false}),supabase.from('movimientos_inventario').select('*,materias_primas(nombre),productos_terminados(nombre)').order('fecha',{ascending:false})]);const b=q.find(x=>x.error);setErr(b?.error?.message||'');if(!b)setD({materias:q[0].data||[],products:q[1].data||[],formulas:q[2].data||[],orders:q[3].data||[],dispatches:q[4].data||[],movimientos:q[5].data||[]});setLoading(false)};useEffect(()=>{load()},[]);const notify=x=>{setToast(x);setTimeout(()=>setToast(''),3000)};if(loading)return <div className="loading"><div className="loader">MAS FERTIL</div><p>Cargando sistema...</p></div>;const title=nav.find(x=>x[0]===tab)[2];return <div className="app"><aside><div className="brand"><div>MAS</div><span>FERTIL SAE</span></div><div className="menu">{nav.map(([id,ic,t])=><button className={tab===id?'active':''} onClick={()=>setTab(id)} key={id}><i>{ic}</i>{t}</button>)}</div><div className="sidefoot">Sistema de Producción<br/>e Inventario</div></aside><main><header><div><div className="crumb">MAS FERTIL SAE / {title}</div><h1>{title}</h1></div><div className="date">{new Date().toLocaleDateString('es-PY')}</div></header>{err&&<div className="error">⚠ {err}</div>}{tab==='dashboard'&&<Dashboard d={d} go={setTab}/>} {tab==='materias'&&<Materias items={d.materias} reload={load} notify={notify} manual={()=>setModal({type:'adjustMP'})}/>} {tab==='produccion'&&<Produccion items={d.orders} add={()=>setModal({type:'production'})}/>} {tab==='formulas'&&<Formulas items={d.formulas} materias={d.materias} reload={load} notify={notify}/>} {tab==='productos'&&<Productos items={d.products} manual={()=>setModal({type:'adjustPT'})}/>} {tab==='despachos'&&<Despachos items={d.dispatches} add={()=>setModal({type:'dispatch'})}/>} {tab==='movimientos'&&<Movimientos items={d.movimientos}/>}</main>{modal?.type==='adjustMP'&&<Ajuste tipo="mp" items={d.materias} close={()=>setModal(null)} reload={load} notify={notify}/>} {modal?.type==='adjustPT'&&<Ajuste tipo="pt" items={d.products} close={()=>setModal(null)} reload={load} notify={notify}/>} {modal?.type==='production'&&<ProduccionModal formulas={d.formulas} close={()=>setModal(null)} reload={load} notify={notify}/>} {modal?.type==='dispatch'&&<DespachoModal products={d.products} close={()=>setModal(null)} reload={load} notify={notify}/>} {toast&&<div className="toast">✓ {toast}</div>}</div>}
const Toolbar=({title,children})=><div className="toolbar"><h2>{title}</h2><div className="actions">{children}</div></div>;const Panel=({title,children})=><section className="panel">{title&&<h3>{title}</h3>}{children}</section>;const Button=({children,onClick,secondary=false})=><button className={secondary?'secondary':'primary'} onClick={onClick}>{children}</button>;const Table=({heads,rows})=><div className="tablewrap"><table><thead><tr>{heads.map(h=><th key={h}>{h}</th>)}</tr></thead><tbody>{rows.map((r,i)=><tr key={i}>{r.map((c,j)=><td key={j}>{c}</td>)}</tr>)}</tbody></table></div>;const Status=({s})=><span className={'status '+s.toLowerCase()}>{s}</span>;const Modal=({title,close,children})=><div className="overlay"><div className="modal"><div className="modalhead"><h2>{title}</h2><button onClick={close}>×</button></div>{children}</div></div>;
function Dashboard({d,go}){let mp=d.materias.reduce((a,x)=>a+Number(x.stock_inicial||0),0),pt=d.products.reduce((a,x)=>a+Number(x.stock_kg||0),0);return <><section className="welcome"><div><h2>Resumen general</h2><p>Control central de producción e inventario.</p></div><Button onClick={()=>go('produccion')}>+ Nueva producción</Button></section><div className="cards"><Card t="Materias primas" v={fmt(mp)+' kg'}/><Card t="Productos terminados" v={fmt(pt)+' kg'}/><Card t="Órdenes" v={d.orders.length}/><Card t="Alertas" v={d.materias.filter(x=>Number(x.stock_inicial)<=Number(x.stock_minimo)).length}/></div><Panel title="Estado de materias primas"><Table heads={['Código','Material','Tipo','Stock','Estado']} rows={d.materias.map(x=>[x.codigo,x.nombre,x.tipo,fmt(x.stock_inicial)+' '+x.unidad,<Status s={estado(x.stock_inicial,x.stock_minimo)}/>])}/></Panel></>};const Card=({t,v})=><div className="card"><div><small>{t}</small><strong>{v}</strong><em>Actual</em></div></div>;
function Materias({items,reload,notify,manual}){return <><Toolbar title="Inventario actual"><Button secondary onClick={manual}>✎ Carga / ajuste manual</Button><Button onClick={manual}>+ Registrar ingreso</Button></Toolbar><Panel><Table heads={['Código','Material','Tipo','Unidad','Stock actual','Mínimo','Estado']} rows={items.map(x=>[x.codigo,x.nombre,x.tipo,x.unidad,fmt(x.stock_inicial),fmt(x.stock_minimo),<Status s={estado(x.stock_inicial,x.stock_minimo)}/>])}/></Panel></>}
function Ajuste({tipo,items,close,reload,notify}){const[id,setId]=useState(''),[op,setOp]=useState('set'),[qty,setQty]=useState(''),[reason,setReason]=useState('');const x=items.find(a=>a.id===Number(id)),cur=x?Number(tipo==='mp'?x.stock_inicial:x.stock_kg):0,q=Number(qty||0),next=op==='set'?q:op==='add'?cur+q:cur-q,diff=next-cur;const save=async()=>{if(!x||q<0||!reason.trim()){notify('Seleccione un artículo, cantidad y justificación');return}if(next<0){notify('El stock no puede quedar negativo');return}let table=tipo==='mp'?'materias_primas':'productos_terminados',field=tipo==='mp'?'stock_inicial':'stock_kg',u=await supabase.from(table).update({[field]:next}).eq('id',x.id);if(u.error)return notify(u.error.message);let m=await supabase.from('movimientos_inventario').insert({tipo:tipo==='mp'?'AJUSTE MANUAL MATERIA PRIMA':'AJUSTE MANUAL PRODUCTO TERMINADO',material_id:tipo==='mp'?x.id:null,producto_id:tipo==='pt'?x.id:null,cantidad_kg:diff,detalle:reason.trim()});if(m.error)return notify(m.error.message);await reload();notify('Ajuste manual registrado y auditado');close()};return <Modal title={tipo==='mp'?'Carga / ajuste manual de materia prima':'Carga / ajuste manual de producto terminado'} close={close}><div className="notice">Permite cargar stock inicial, corregir inventario físico o registrar diferencias. <b>La justificación es obligatoria.</b></div><label>{tipo==='mp'?'Materia prima':'Producto terminado'}<select value={id} onChange={e=>setId(e.target.value)}><option value="">Seleccionar...</option>{items.map(a=><option value={a.id} key={a.id}>{a.codigo} — {a.nombre}</option>)}</select></label>{x&&<div className="current">Stock actual: <strong>{fmt(cur)} kg</strong></div>}<label>Operación<select value={op} onChange={e=>setOp(e.target.value)}><option value="set">Establecer stock exacto</option><option value="add">Agregar kg</option><option value="sub">Descontar kg</option></select></label><label>{op==='set'?'Nuevo stock (kg)':'Cantidad (kg)'}<input type="number" min="0" value={qty} onChange={e=>setQty(e.target.value)}/></label>{x&&<div className={'calc '+(next<0?'bad':'')}>Stock resultante: <strong>{fmt(next)} kg</strong></div>}<label>Justificación / motivo<textarea rows="3" value={reason} onChange={e=>setReason(e.target.value)} placeholder="Ej.: inventario físico, diferencia de pesaje, carga inicial..."/></label><Button onClick={save}>Guardar ajuste</Button></Modal>}
function Formulas({items,materias,reload,notify}){const[open,setOpen]=useState(false);return <><Toolbar title="Fórmulas"><Button onClick={()=>setOpen(true)}>+ Nueva fórmula</Button></Toolbar><Panel><Table heads={['Código','Fórmula','Línea','Componentes','Total']} rows={items.map(f=><FormulaRow f={f}/>)}/></Panel>{open&&<FormulaModal materias={materias} close={()=>setOpen(false)} reload={reload} notify={notify}/>}</>};function FormulaRow({f}){const[c,setC]=useState([]);useEffect(()=>{supabase.from('formula_componentes').select('kg_por_1000kg,materias_primas(nombre)').eq('formula_id',f.id).then(r=>setC(r.data||[]))},[f.id]);return <tr><td>{f.codigo}</td><td>{f.nombre}</td><td>{f.linea}</td><td>{c.map(x=>x.materias_primas?.nombre+' ('+fmt(x.kg_por_1000kg)+' kg)').join(' · ')||'Sin componentes'}</td><td>{fmt(c.reduce((a,x)=>a+Number(x.kg_por_1000kg||0),0))} kg</td></tr>}
function FormulaModal({materias,close,reload,notify}){const[n,setN]=useState(''),[line,setLine]=useState('Mezcla'),[r,setR]=useState(Array(4).fill(0).map(()=>({m:'',kg:''}))),total=r.reduce((a,x)=>a+Number(x.kg||0),0);const save=async()=>{if(!n||Math.abs(total-1000)>.01)return notify('La fórmula debe sumar exactamente 1.000 kg');let f=await supabase.from('formulas').insert({codigo:'F-'+Date.now().toString().slice(-6),nombre:n,linea:line}).select().single();if(f.error)return notify(f.error.message);let c=await supabase.from('formula_componentes').insert(r.filter(x=>x.m&&Number(x.kg)>0).map(x=>({formula_id:f.data.id,materia_prima_id:Number(x.m),kg_por_1000kg:Number(x.kg)})));if(c.error){await supabase.from('formulas').delete().eq('id',f.data.id);return notify(c.error.message)}await reload();notify('Fórmula creada');close()};return <Modal title="Nueva fórmula" close={close}><label>Nombre<input value={n} onChange={e=>setN(e.target.value)}/></label><label>Línea<select value={line} onChange={e=>setLine(e.target.value)}><option>Mezcla</option><option>NPK</option></select></label>{r.map((x,i)=><div className="formgrid" key={i}><select value={x.m} onChange={e=>setR(v=>v.map((z,j)=>j===i?{...z,m:e.target.value}:z))}><option value="">Materia prima...</option>{materias.map(a=><option value={a.id} key={a.id}>{a.nombre}</option>)}</select><input type="number" value={x.kg} onChange={e=>setR(v=>v.map((z,j)=>j===i?{...z,kg:e.target.value}:z))}/></div>)}<div className="calc">Total: <strong>{fmt(total)} kg</strong></div><Button onClick={save}>Guardar fórmula</Button></Modal>}
function Produccion({items,add}){return <><Toolbar title="Registro de órdenes"><Button onClick={add}>+ Nueva orden</Button></Toolbar><Panel><Table heads={['Orden','Fecha','Fórmula','Línea','Presentación','Bultos','Total kg']} rows={items.map(o=>['OP-'+String(o.numero??o.id).padStart(5,'0'),o.fecha,o.formulas?.nombre||'-',o.linea,o.presentacion,fmt(o.bultos),fmt(o.kg_total)])}/>{!items.length&&<div className="empty">No hay órdenes registradas.</div>}</Panel></>};function ProduccionModal({formulas,close,reload,notify}){const[f,setF]=useState(''),[line,setLine]=useState('Mezcla'),[p,setP]=useState('Big Bag 1000 kg'),[b,setB]=useState(1),kg=b*peso[p];const save=async()=>{let r=await supabase.rpc('registrar_produccion',{p_formula_id:Number(f),p_linea:line,p_presentacion:p,p_bultos:Number(b),p_peso_bulto_kg:peso[p]});if(r.error)return notify(r.error.message);await reload();notify('Producción registrada y consumo automático aplicado');close()};return <Modal title="Nueva orden de producción" close={close}><label>Fórmula<select value={f} onChange={e=>setF(e.target.value)}><option value="">Seleccionar...</option>{formulas.map(x=><option value={x.id} key={x.id}>{x.codigo} — {x.nombre}</option>)}</select></label><label>Línea<select value={line} onChange={e=>setLine(e.target.value)}><option>Mezcla</option><option>NPK</option></select></label><label>Presentación<select value={p} onChange={e=>setP(e.target.value)}><option>Big Bag 1000 kg</option><option>Bolsa 50 kg</option><option>Bolsa 25 kg</option></select></label><label>Bultos<input type="number" min="1" value={b} onChange={e=>setB(Number(e.target.value))}/></label><div className="calc">Producción: <strong>{fmt(kg)} kg</strong></div><Button onClick={save}>Confirmar producción</Button></Modal>}
function Productos({items,manual}){return <><Toolbar title="Stock de productos terminados"><Button secondary onClick={manual}>✎ Carga / ajuste manual</Button></Toolbar><Panel><Table heads={['Código','Producto','Stock kg','Big Bags','Bolsas 50 kg','Bolsas 25 kg','Estado']} rows={items.map(p=>{let s=Number(p.stock_kg||0);return[p.codigo,p.nombre,fmt(s),Math.floor(s/1000),Math.floor(s/50),Math.floor(s/25),<Status s={estado(s,p.stock_minimo_kg)}/>]})}/></Panel></>};function Despachos({items,add}){return <><Toolbar title="Salidas de productos terminados"><Button onClick={add}>+ Nuevo despacho</Button></Toolbar><Panel><Table heads={['Despacho','Fecha','Producto','Presentación','Bultos','Total kg','Cliente']} rows={items.map(d=>['DS-'+String(d.numero??d.id).padStart(5,'0'),d.fecha,d.productos_terminados?.nombre||'-',d.presentacion,fmt(d.bultos),fmt(d.kg_total),d.cliente||'-'])}/></Panel></>};function DespachoModal({products,close,reload,notify}){const[p,setP]=useState(''),[pr,setPr]=useState('Big Bag 1000 kg'),[b,setB]=useState(1),[c,setC]=useState(''),kg=b*peso[pr];const save=async()=>{let r=await supabase.rpc('registrar_despacho',{p_producto_id:Number(p),p_presentacion:pr,p_bultos:Number(b),p_peso_bulto_kg:peso[pr],p_cliente:c||null,p_observacion:null});if(r.error)return notify(r.error.message);await reload();notify('Despacho registrado');close()};return <Modal title="Nuevo despacho" close={close}><label>Producto<select value={p} onChange={e=>setP(e.target.value)}><option value="">Seleccionar...</option>{products.map(x=><option value={x.id} key={x.id}>{x.codigo} — {x.nombre} ({fmt(x.stock_kg)} kg)</option>)}</select></label><label>Presentación<select value={pr} onChange={e=>setPr(e.target.value)}><option>Big Bag 1000 kg</option><option>Bolsa 50 kg</option><option>Bolsa 25 kg</option></select></label><label>Bultos<input type="number" min="1" value={b} onChange={e=>setB(Number(e.target.value))}/></label><label>Cliente<input value={c} onChange={e=>setC(e.target.value)}/></label><div className="calc">Salida: <strong>{fmt(kg)} kg</strong></div><Button onClick={save}>Confirmar despacho</Button></Modal>};function Movimientos({items}){return <><Toolbar title="Historial de movimientos"/><Panel><Table heads={['Fecha','Tipo','Referencia','Detalle','Cantidad']} rows={items.map(m=>[new Date(m.fecha).toLocaleString('es-PY'),m.tipo,m.referencia||'-',m.detalle||'-',fmt(m.cantidad_kg)+' kg'])}/></Panel></>}
createRoot(document.getElementById('root')).render(<App/>);